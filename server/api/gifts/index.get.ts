// This file defines the gift listing endpoint.
// It should later support filters, visibility checks, and wishlist-scoped queries.
export default defineEventHandler(() => {
  return {
    success: true,
    message: 'Gift list placeholder response.',
    data: []
  }
})
