import { defineComponent, computed, mergeProps, unref, useSSRContext, ref, watch, withCtx, createTextVNode } from "vue";
import { ssrRenderAttrs, ssrRenderAttr, ssrInterpolate, ssrRenderClass, ssrRenderStyle, ssrRenderList, ssrIncludeBooleanAttr, ssrRenderComponent, ssrLooseContain, ssrLooseEqual } from "vue/server-renderer";
import { _ as _export_sfc, u as useNuxtApp, b as useRoute, a as useRouter } from "../server.mjs";
import { _ as __nuxt_component_0$2 } from "./nuxt-link-B1pdB3FY.js";
import { _ as __nuxt_component_2 } from "./WishlistForm-DoDgGF9z.js";
import { s as splitWishlistTitle, f as formatWishlistVisibility, a as formatParticipantJoinMode, b as formatGiftReservationSetting, c as formatGiftStatusVisibility, d as formatParticipantStatus } from "./helpers-BT2bhxkr.js";
import { g as getWishlistThemeStyle, f as formatWishlistScenario, d as detectWishlistScenario } from "./wishlistScenarios-Bq5lbsHk.js";
import { u as useAuth } from "./useAuth-D55glZy3.js";
import { u as useFriends } from "./useFriends-6-ja2yAP.js";
import { u as useState } from "./state-DQnwRar-.js";
import { u as useWishlists } from "./useWishlists-DPZKeTte.js";
import "/Users/anna/Documents/NAU/8 sem/diploma/program code/node_modules/ofetch/dist/node.mjs";
import "#internal/nuxt/paths";
import "/Users/anna/Documents/NAU/8 sem/diploma/program code/node_modules/hookable/dist/index.mjs";
import "/Users/anna/Documents/NAU/8 sem/diploma/program code/node_modules/unctx/dist/index.mjs";
import "/Users/anna/Documents/NAU/8 sem/diploma/program code/node_modules/@nuxt/nitro-server/dist/runtime/h3-compat.mjs";
import "vue-router";
import "/Users/anna/Documents/NAU/8 sem/diploma/program code/node_modules/defu/dist/defu.mjs";
import "/Users/anna/Documents/NAU/8 sem/diploma/program code/node_modules/ufo/dist/index.mjs";
const _sfc_main$3 = /* @__PURE__ */ defineComponent({
  __name: "GiftCard",
  __ssrInlineRender: true,
  props: {
    gift: {},
    canManage: { type: Boolean, default: false },
    reservationMessage: { default: "" },
    reservationActionLabel: { default: "Reserve gift" },
    canReserve: { type: Boolean, default: false },
    reserveDisabled: { type: Boolean, default: false },
    reserveFullyActionLabel: { default: "Reserve fully" },
    canReserveFully: { type: Boolean, default: false },
    reserveFullyDisabled: { type: Boolean, default: false },
    canCancelReservation: { type: Boolean, default: false },
    contributionSummary: { default: null }
  },
  emits: ["edit", "delete", "reserve", "reserveFully", "cancelReservation"],
  setup(__props) {
    const props = __props;
    const currencySymbolMap = {
      UAH: "₴",
      USD: "$",
      EUR: "€"
    };
    const priorityLabel = computed(() => {
      if (props.gift.priority === "high") {
        return "High priority";
      }
      if (props.gift.priority === "low") {
        return "Low priority";
      }
      return "Medium priority";
    });
    const formattedPrice = computed(() => {
      if (props.gift.price == null) {
        return "Price not specified";
      }
      const symbol = currencySymbolMap[props.gift.currency] || "₴";
      return `${props.gift.price} ${symbol}`;
    });
    const contributionProgressStyle = computed(() => {
      const progress = props.contributionSummary?.progressPercent ?? 0;
      return {
        width: `${Math.max(0, Math.min(100, progress))}%`
      };
    });
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<article${ssrRenderAttrs(mergeProps({ class: "surface-card gift-card" }, _attrs))} data-v-df4b9d6b><div class="gift-card__media" data-v-df4b9d6b>`);
      if (__props.gift.imageUrl) {
        _push(`<img${ssrRenderAttr("src", __props.gift.imageUrl)}${ssrRenderAttr("alt", __props.gift.title)} class="gift-card__image" data-v-df4b9d6b>`);
      } else {
        _push(`<div class="gift-card__image gift-card__image--placeholder" data-v-df4b9d6b> No photo </div>`);
      }
      _push(`<p class="gift-card__price" data-v-df4b9d6b>${ssrInterpolate(unref(formattedPrice))}</p><span class="${ssrRenderClass([`gift-card__priority--${__props.gift.priority}`, "gift-card__priority"])}" data-v-df4b9d6b>${ssrInterpolate(unref(priorityLabel))}</span></div><div class="gift-card__content" data-v-df4b9d6b><div class="gift-card__header" data-v-df4b9d6b><h3 class="gift-card__title" data-v-df4b9d6b>${ssrInterpolate(__props.gift.title)}</h3></div><p class="gift-card__description" data-v-df4b9d6b>${ssrInterpolate(__props.gift.description || "No description yet.")}</p>`);
      if (__props.reservationMessage) {
        _push(`<p class="gift-card__reservation" data-v-df4b9d6b>${ssrInterpolate(__props.reservationMessage)}</p>`);
      } else {
        _push(`<!---->`);
      }
      if (__props.contributionSummary) {
        _push(`<div class="gift-card__contributions" data-v-df4b9d6b><div class="gift-card__contributions-header" data-v-df4b9d6b><p data-v-df4b9d6b><strong data-v-df4b9d6b>Goal:</strong> ${ssrInterpolate(__props.contributionSummary.targetLabel)}</p><p data-v-df4b9d6b><strong data-v-df4b9d6b>Collected:</strong> ${ssrInterpolate(__props.contributionSummary.collectedLabel)}</p></div><div class="gift-card__progress" data-v-df4b9d6b><div class="gift-card__progress-bar" style="${ssrRenderStyle(unref(contributionProgressStyle))}" data-v-df4b9d6b></div></div><p class="gift-card__contributions-meta" data-v-df4b9d6b>${ssrInterpolate(__props.contributionSummary.contributorCount)} ${ssrInterpolate(__props.contributionSummary.contributorCount === 1 ? "contributor" : "contributors")}</p>`);
        if (__props.contributionSummary.statusNote) {
          _push(`<p class="gift-card__contributions-note" data-v-df4b9d6b>${ssrInterpolate(__props.contributionSummary.statusNote)}</p>`);
        } else {
          _push(`<!---->`);
        }
        if (__props.contributionSummary.contributions.length) {
          _push(`<ul class="gift-card__contribution-list" data-v-df4b9d6b><!--[-->`);
          ssrRenderList(__props.contributionSummary.contributions, (contribution) => {
            _push(`<li class="gift-card__contribution-item" data-v-df4b9d6b><div data-v-df4b9d6b><strong data-v-df4b9d6b>${ssrInterpolate(contribution.name)}</strong>`);
            if (contribution.username) {
              _push(`<p data-v-df4b9d6b>@${ssrInterpolate(contribution.username)}</p>`);
            } else {
              _push(`<!---->`);
            }
            _push(`</div><span data-v-df4b9d6b>${ssrInterpolate(contribution.amountLabel)}</span></li>`);
          });
          _push(`<!--]--></ul>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      if (__props.canManage) {
        _push(`<div class="gift-card__actions" data-v-df4b9d6b><button type="button" data-v-df4b9d6b> Edit </button><button type="button" class="gift-card__danger" data-v-df4b9d6b> Delete </button></div>`);
      } else if (__props.canReserve || __props.canCancelReservation) {
        _push(`<div class="gift-card__actions" data-v-df4b9d6b>`);
        if (__props.canReserve) {
          _push(`<button type="button"${ssrIncludeBooleanAttr(__props.reserveDisabled) ? " disabled" : ""} data-v-df4b9d6b>${ssrInterpolate(__props.reservationActionLabel)}</button>`);
        } else {
          _push(`<!---->`);
        }
        if (__props.canReserveFully) {
          _push(`<button type="button" class="gift-card__secondary"${ssrIncludeBooleanAttr(__props.reserveFullyDisabled) ? " disabled" : ""} data-v-df4b9d6b>${ssrInterpolate(__props.reserveFullyActionLabel)}</button>`);
        } else {
          _push(`<!---->`);
        }
        if (__props.canCancelReservation) {
          _push(`<button type="button" class="gift-card__secondary" data-v-df4b9d6b> Cancel reservation </button>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      _push(`</div></article>`);
    };
  }
});
const _sfc_setup$3 = _sfc_main$3.setup;
_sfc_main$3.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/gift/GiftCard.vue");
  return _sfc_setup$3 ? _sfc_setup$3(props, ctx) : void 0;
};
const __nuxt_component_0$1 = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$3, [["__scopeId", "data-v-df4b9d6b"]]), { __name: "GiftCard" });
const _sfc_main$2 = /* @__PURE__ */ defineComponent({
  __name: "GiftList",
  __ssrInlineRender: true,
  props: {
    gifts: {},
    canManage: { type: Boolean, default: false },
    manageableGiftIds: { default: () => [] },
    giftStates: { default: () => ({}) },
    emptyMessage: { default: "No gifts have been added yet." }
  },
  emits: ["edit", "delete", "reserve", "reserveFully", "cancelReservation"],
  setup(__props) {
    return (_ctx, _push, _parent, _attrs) => {
      const _component_GiftCard = __nuxt_component_0$1;
      _push(`<section${ssrRenderAttrs(mergeProps({ class: "gift-list" }, _attrs))} data-v-debbbb99>`);
      if (__props.gifts.length) {
        _push(`<div class="gift-list__items" data-v-debbbb99><!--[-->`);
        ssrRenderList(__props.gifts, (gift) => {
          _push(ssrRenderComponent(_component_GiftCard, {
            key: gift.id,
            gift,
            "can-manage": __props.canManage || __props.manageableGiftIds.includes(gift.id),
            "reservation-message": __props.giftStates[gift.id]?.reservationMessage,
            "reservation-action-label": __props.giftStates[gift.id]?.reservationActionLabel,
            "can-reserve": __props.giftStates[gift.id]?.canReserve,
            "reserve-disabled": __props.giftStates[gift.id]?.reserveDisabled,
            "reserve-fully-action-label": __props.giftStates[gift.id]?.reserveFullyActionLabel,
            "can-reserve-fully": __props.giftStates[gift.id]?.canReserveFully,
            "reserve-fully-disabled": __props.giftStates[gift.id]?.reserveFullyDisabled,
            "can-cancel-reservation": __props.giftStates[gift.id]?.canCancelReservation,
            "contribution-summary": __props.giftStates[gift.id]?.contributionSummary,
            onEdit: ($event) => _ctx.$emit("edit", $event),
            onDelete: ($event) => _ctx.$emit("delete", $event),
            onReserve: ($event) => _ctx.$emit("reserve", $event),
            onReserveFully: ($event) => _ctx.$emit("reserveFully", $event),
            onCancelReservation: ($event) => _ctx.$emit("cancelReservation", $event)
          }, null, _parent));
        });
        _push(`<!--]--></div>`);
      } else {
        _push(`<p data-v-debbbb99>${ssrInterpolate(__props.emptyMessage)}</p>`);
      }
      _push(`</section>`);
    };
  }
});
const _sfc_setup$2 = _sfc_main$2.setup;
_sfc_main$2.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/gift/GiftList.vue");
  return _sfc_setup$2 ? _sfc_setup$2(props, ctx) : void 0;
};
const __nuxt_component_0 = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$2, [["__scopeId", "data-v-debbbb99"]]), { __name: "GiftList" });
const titleMaxLength = 80;
const descriptionMaxLength = 300;
const _sfc_main$1 = /* @__PURE__ */ defineComponent({
  __name: "GiftForm",
  __ssrInlineRender: true,
  props: {
    initialValues: { default: () => ({}) },
    submitLabel: { default: "Save gift" },
    errorMessage: { default: "" }
  },
  emits: ["save", "cancel"],
  setup(__props, { emit: __emit }) {
    const currencyOptions = [
      { value: "UAH", label: "Hryvnia (₴)" },
      { value: "USD", label: "Dollar ($)" },
      { value: "EUR", label: "Euro (€)" }
    ];
    const props = __props;
    const defaultValues = {
      currency: "UAH",
      priority: "medium"
    };
    const title = ref("");
    const description = ref("");
    const imageUrl = ref("");
    const price = ref("");
    const currency = ref("UAH");
    const priority = ref("medium");
    const imageSourceLabel = ref("");
    const localErrorMessage = ref("");
    const remainingTitleCharacters = computed(() => {
      return titleMaxLength - title.value.length;
    });
    const remainingDescriptionCharacters = computed(() => {
      return descriptionMaxLength - description.value.length;
    });
    const imagePreview = computed(() => {
      return imageUrl.value.trim();
    });
    const applyInitialValues = () => {
      title.value = (props.initialValues.title || "").slice(0, titleMaxLength);
      description.value = (props.initialValues.description || "").slice(0, descriptionMaxLength);
      imageUrl.value = props.initialValues.imageUrl || "";
      price.value = props.initialValues.price != null ? String(props.initialValues.price) : "";
      currency.value = props.initialValues.currency || defaultValues.currency;
      priority.value = props.initialValues.priority || defaultValues.priority;
      imageSourceLabel.value = imageUrl.value ? "link" : "";
    };
    watch(() => props.initialValues, applyInitialValues, { deep: true, immediate: true });
    watch(() => title.value, () => {
      if (title.value.trim()) {
        localErrorMessage.value = "";
      }
    });
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<form${ssrRenderAttrs(mergeProps({
        class: "surface-card gift-form",
        novalidate: ""
      }, _attrs))} data-v-9735752e><h2 data-v-9735752e>Gift details</h2><label class="gift-form__field" data-v-9735752e><span data-v-9735752e>Gift name</span><input${ssrRenderAttr("value", unref(title))} type="text" required${ssrRenderAttr("maxlength", titleMaxLength)} placeholder="Headphones, perfume, Lego set" data-v-9735752e></label><p class="gift-form__counter" data-v-9735752e>${ssrInterpolate(unref(remainingTitleCharacters))} characters left </p><label class="gift-form__field" data-v-9735752e><span data-v-9735752e>Description</span><textarea rows="3"${ssrRenderAttr("maxlength", descriptionMaxLength)} placeholder="Add details like size, color, brand, or why you want this gift" data-v-9735752e>${ssrInterpolate(unref(description))}</textarea></label><p class="gift-form__counter" data-v-9735752e>${ssrInterpolate(unref(remainingDescriptionCharacters))} characters left </p><div class="gift-form__field" data-v-9735752e><span data-v-9735752e>Photo</span>`);
      if (unref(imagePreview)) {
        _push(`<div class="gift-form__preview" data-v-9735752e><img${ssrRenderAttr("src", unref(imagePreview))} alt="Gift preview" class="gift-form__preview-image" data-v-9735752e><div class="gift-form__preview-meta" data-v-9735752e><p data-v-9735752e> Source: ${ssrInterpolate(unref(imageSourceLabel) === "upload" ? "uploaded file" : "internet link")}</p><button type="button" class="gift-form__secondary-button" data-v-9735752e> Remove image </button></div></div>`);
      } else {
        _push(`<!---->`);
      }
      _push(`<label class="gift-form__upload" data-v-9735752e><span data-v-9735752e>Upload from device</span><input type="file" accept="image/*" data-v-9735752e></label><label class="gift-form__field" data-v-9735752e><span data-v-9735752e>Image link from the internet</span><input${ssrRenderAttr("value", unref(imageUrl))} type="url" placeholder="https://example.com/gift-photo.jpg" data-v-9735752e></label></div><div class="gift-form__price-row" data-v-9735752e><label class="gift-form__field" data-v-9735752e><span data-v-9735752e>Price</span><input${ssrRenderAttr("value", unref(price))} type="number" min="0" step="0.01" placeholder="1500" data-v-9735752e></label><label class="gift-form__field" data-v-9735752e><span data-v-9735752e>Currency</span><select data-v-9735752e><!--[-->`);
      ssrRenderList(currencyOptions, (option) => {
        _push(`<option${ssrRenderAttr("value", option.value)} data-v-9735752e${ssrIncludeBooleanAttr(Array.isArray(unref(currency)) ? ssrLooseContain(unref(currency), option.value) : ssrLooseEqual(unref(currency), option.value)) ? " selected" : ""}>${ssrInterpolate(option.label)}</option>`);
      });
      _push(`<!--]--></select></label></div><div class="gift-form__field" data-v-9735752e><span data-v-9735752e>Priority</span><div class="gift-form__priority-options" data-v-9735752e><button type="button" class="${ssrRenderClass([{ "gift-form__priority-button--active": unref(priority) === "low" }, "gift-form__priority-button gift-form__priority-button--low"])}" data-v-9735752e> Low </button><button type="button" class="${ssrRenderClass([{ "gift-form__priority-button--active": unref(priority) === "medium" }, "gift-form__priority-button gift-form__priority-button--medium"])}" data-v-9735752e> Medium </button><button type="button" class="${ssrRenderClass([{ "gift-form__priority-button--active": unref(priority) === "high" }, "gift-form__priority-button gift-form__priority-button--high"])}" data-v-9735752e> High </button></div></div><div class="gift-form__actions" data-v-9735752e><button type="button" class="gift-form__submit" data-v-9735752e>${ssrInterpolate(__props.submitLabel)}</button><button type="button" class="gift-form__secondary-button" data-v-9735752e> Cancel </button></div>`);
      if (unref(localErrorMessage) || __props.errorMessage) {
        _push(`<p class="gift-form__error" data-v-9735752e>${ssrInterpolate(unref(localErrorMessage) || __props.errorMessage)}</p>`);
      } else {
        _push(`<!---->`);
      }
      _push(`</form>`);
    };
  }
});
const _sfc_setup$1 = _sfc_main$1.setup;
_sfc_main$1.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/gift/GiftForm.vue");
  return _sfc_setup$1 ? _sfc_setup$1(props, ctx) : void 0;
};
const __nuxt_component_3 = /* @__PURE__ */ Object.assign(_export_sfc(_sfc_main$1, [["__scopeId", "data-v-9735752e"]]), { __name: "GiftForm" });
const mapPriorityFromDb = (priority) => {
  if (priority === 3) {
    return "high";
  }
  if (priority === 1) {
    return "low";
  }
  return "medium";
};
const mapPriorityToDb = (priority) => {
  if (priority === "high") {
    return 3;
  }
  if (priority === "low") {
    return 1;
  }
  return 2;
};
const mapCurrencyFromDb = (currency) => {
  if (currency === "USD" || currency === "EUR") {
    return currency;
  }
  return "UAH";
};
const mapGift = (gift) => ({
  id: gift.id,
  wishlistId: gift.wishlist_id,
  createdBy: gift.created_by,
  title: gift.title,
  description: gift.description,
  imageUrl: gift.image_url,
  price: gift.price,
  currency: mapCurrencyFromDb(gift.currency),
  priority: mapPriorityFromDb(gift.priority),
  createdAt: gift.created_at,
  updatedAt: gift.updated_at
});
const mapReservation = (reservation) => ({
  id: reservation.id,
  giftId: reservation.gift_id,
  reservedBy: reservation.reserved_by,
  status: reservation.status,
  isAnonymous: reservation.is_anonymous,
  reservedAt: reservation.reserved_at,
  updatedAt: reservation.updated_at
});
const mapContribution = (contribution) => ({
  id: contribution.id,
  giftId: contribution.gift_id,
  contributorId: contribution.contributor_id,
  reservationId: contribution.reservation_id,
  amount: contribution.amount,
  currency: mapCurrencyFromDb(contribution.currency),
  status: contribution.status,
  note: contribution.note,
  contributorProfile: contribution.contributor_profile ? {
    id: contribution.contributor_profile.id,
    fullName: contribution.contributor_profile.full_name,
    username: contribution.contributor_profile.username,
    avatarUrl: contribution.contributor_profile.avatar_url,
    email: contribution.contributor_profile.email
  } : null,
  createdAt: contribution.created_at,
  updatedAt: contribution.updated_at
});
const buildGiftPayload = (payload) => ({
  title: payload.title.trim(),
  description: payload.description?.trim() || null,
  image_url: payload.imageUrl?.trim() || null,
  price: payload.price ?? null,
  currency: payload.currency || "UAH",
  priority: mapPriorityToDb(payload.priority),
  updated_at: (/* @__PURE__ */ new Date()).toISOString()
});
const useGifts = () => {
  const { $supabase } = useNuxtApp();
  const gifts = useState("gifts", () => []);
  const getGiftReservations = async (giftIds) => {
    if (!giftIds.length) {
      return {
        data: [],
        error: null
      };
    }
    const response = await $supabase.from("gift_reservations").select("*").in("gift_id", giftIds).in("status", ["reserved", "purchased"]);
    return {
      data: (response.data || []).map(mapReservation),
      error: response.error
    };
  };
  const getGiftContributions = async (giftIds) => {
    if (!giftIds.length) {
      return {
        data: [],
        error: null
      };
    }
    const response = await $supabase.from("gift_contributions").select(`
        *,
        contributor_profile:profiles!gift_contributions_contributor_id_fkey (
          id,
          full_name,
          username,
          avatar_url,
          email
        )
      `).in("gift_id", giftIds).in("status", ["pledged", "paid"]);
    return {
      data: (response.data || []).map(mapContribution),
      error: response.error
    };
  };
  const getWishlistGifts = async (wishlistId) => {
    const response = await $supabase.from("wishlist_gifts").select("*").eq("wishlist_id", wishlistId).eq("is_deleted", false).order("priority", { ascending: false }).order("created_at", { ascending: false });
    const mappedGifts = (response.data || []).map(mapGift);
    if (!response.error) {
      gifts.value = mappedGifts;
    }
    return {
      data: mappedGifts,
      error: response.error
    };
  };
  const createGift = async (wishlistId, userId, payload) => {
    const response = await $supabase.from("wishlist_gifts").insert({
      wishlist_id: wishlistId,
      created_by: userId,
      quantity: 1,
      ...buildGiftPayload(payload)
    }).select("*").single();
    return {
      data: response.data ? mapGift(response.data) : null,
      error: response.error
    };
  };
  const updateGift = async (giftId, payload) => {
    const response = await $supabase.from("wishlist_gifts").update(buildGiftPayload(payload)).eq("id", giftId).select("*").single();
    return {
      data: response.data ? mapGift(response.data) : null,
      error: response.error
    };
  };
  const deleteGift = async (giftId) => {
    const response = await $supabase.from("wishlist_gifts").update({
      is_deleted: true,
      updated_at: (/* @__PURE__ */ new Date()).toISOString()
    }).eq("id", giftId);
    return {
      error: response.error
    };
  };
  const reserveGift = async (giftId, userId) => {
    const response = await $supabase.from("gift_reservations").insert({
      gift_id: giftId,
      reserved_by: userId,
      status: "reserved",
      is_anonymous: false,
      updated_at: (/* @__PURE__ */ new Date()).toISOString()
    }).select("*").single();
    return {
      data: response.data ? mapReservation(response.data) : null,
      error: response.error
    };
  };
  const cancelReservation = async (giftId, userId) => {
    const response = await $supabase.from("gift_reservations").delete().eq("gift_id", giftId).eq("reserved_by", userId);
    return {
      error: response.error
    };
  };
  const createContribution = async (giftId, contributorId, amount, currency, note) => {
    const response = await $supabase.from("gift_contributions").insert({
      gift_id: giftId,
      contributor_id: contributorId,
      amount,
      currency,
      note: note?.trim() || null,
      status: "pledged",
      updated_at: (/* @__PURE__ */ new Date()).toISOString()
    }).select("*").single();
    return {
      data: response.data ? mapContribution(response.data) : null,
      error: response.error
    };
  };
  const cancelContribution = async (giftId, contributorId) => {
    const response = await $supabase.from("gift_contributions").delete().eq("gift_id", giftId).eq("contributor_id", contributorId);
    return {
      error: response.error
    };
  };
  const clearSplitStateForWishlist = async (wishlistId) => {
    const rpcResponse = await $supabase.rpc("clear_split_state_for_wishlist", {
      target_wishlist_id: wishlistId
    });
    if (!rpcResponse.error) {
      return {
        error: null
      };
    }
    const rpcMissing = rpcResponse.error.message?.toLowerCase().includes("could not find the function") || rpcResponse.error.message?.toLowerCase().includes("function public.clear_split_state_for_wishlist");
    if (!rpcMissing) {
      return {
        error: rpcResponse.error
      };
    }
    const giftsResponse = await $supabase.from("wishlist_gifts").select("id").eq("wishlist_id", wishlistId).eq("is_deleted", false);
    if (giftsResponse.error) {
      return {
        error: giftsResponse.error
      };
    }
    const giftIds = (giftsResponse.data || []).map((gift) => gift.id);
    if (!giftIds.length) {
      return {
        error: null
      };
    }
    const contributionRowsResponse = await $supabase.from("gift_contributions").select("gift_id, contributor_id").in("gift_id", giftIds);
    if (contributionRowsResponse.error) {
      return {
        error: contributionRowsResponse.error
      };
    }
    const contributedGiftIds = [...new Set((contributionRowsResponse.data || []).map((row) => row.gift_id))];
    if (!contributedGiftIds.length) {
      return {
        error: null
      };
    }
    const reservationsResponse = await $supabase.from("gift_reservations").delete().in("gift_id", contributedGiftIds);
    if (reservationsResponse.error) {
      return {
        error: reservationsResponse.error
      };
    }
    const verificationResponse = await $supabase.from("gift_reservations").select("id, gift_id").in("gift_id", contributedGiftIds);
    if (verificationResponse.error) {
      return {
        error: verificationResponse.error
      };
    }
    if ((verificationResponse.data || []).length > 0) {
      return {
        error: {
          message: "Some split reservations could not be removed because the current database policies do not allow deleting other users’ reservations. Add the clear_split_state_for_wishlist RPC helper in Supabase to fix this."
        }
      };
    }
    const contributionsResponse = await $supabase.from("gift_contributions").delete().in("gift_id", contributedGiftIds);
    if (contributionsResponse.error) {
      return {
        error: contributionsResponse.error
      };
    }
    return {
      error: null
    };
  };
  return {
    gifts,
    getWishlistGifts,
    getGiftReservations,
    getGiftContributions,
    createGift,
    updateGift,
    deleteGift,
    reserveGift,
    cancelReservation,
    createContribution,
    cancelContribution,
    clearSplitStateForWishlist
  };
};
const _sfc_main = /* @__PURE__ */ defineComponent({
  __name: "[id]",
  __ssrInlineRender: true,
  setup(__props) {
    const route = useRoute();
    const router = useRouter();
    const statusMessage = ref("");
    const wishlistValues = ref({});
    const settingsFormKey = ref(0);
    const wishlist = ref(null);
    const gifts = ref([]);
    const reservations = ref([]);
    const contributions = ref([]);
    const participants = ref([]);
    const assignments = ref([]);
    const manualAssignmentSelections = ref({});
    const inviteUsername = ref("");
    const isLoaded = ref(false);
    const currentUserId = ref("");
    const currentUserEmail = ref("");
    const ownerProfile = ref(null);
    const isOwner = ref(false);
    const userParticipation = ref(null);
    const giftValues = ref({});
    const editingGiftId = ref("");
    const giftStatusMessage = ref("");
    const showSettingsModal = ref(false);
    const showGiftModal = ref(false);
    const showContributionModal = ref(false);
    const showDistributionConfirmModal = ref(false);
    const showActionConfirmModal = ref(false);
    const showOwnerAssignments = ref(false);
    const selectedContributionGift = ref(null);
    const contributionAmount = ref("");
    const contributionNote = ref("");
    const distributionConfirmMode = ref("generate");
    const actionConfirmMode = ref("deleteWishlist");
    const actionConfirmGiftTitle = ref("");
    const pendingDeleteGiftId = ref("");
    const currencySymbolMap = {
      UAH: "₴",
      USD: "$",
      EUR: "€"
    };
    const { getUser } = useAuth();
    const { getProfilesByIds } = useFriends();
    const {
      getWishlistGifts,
      getGiftReservations,
      getGiftContributions,
      createGift,
      updateGift,
      reserveGift,
      cancelReservation,
      cancelContribution,
      clearSplitStateForWishlist
    } = useGifts();
    const {
      getWishlistById,
      getWishlistAssignments,
      getOwnWishlistAssignment,
      updateWishlist,
      getWishlistParticipants,
      getUserParticipation
    } = useWishlists();
    const pendingParticipants = computed(() => {
      return participants.value.filter((participant) => participant.status === "pending");
    });
    const invitedParticipants = computed(() => {
      return participants.value.filter((participant) => participant.status === "invited" || participant.status === "approved");
    });
    const giftFormHeading = computed(() => {
      return editingGiftId.value ? "Edit gift" : "Add a gift";
    });
    const reservationsByGiftId = computed(() => {
      return reservations.value.reduce((accumulator, reservation) => {
        if (!accumulator[reservation.giftId]) {
          accumulator[reservation.giftId] = [];
        }
        accumulator[reservation.giftId].push(reservation);
        return accumulator;
      }, {});
    });
    const contributionsByGiftId = computed(() => {
      return contributions.value.reduce((accumulator, contribution) => {
        if (!accumulator[contribution.giftId]) {
          accumulator[contribution.giftId] = [];
        }
        accumulator[contribution.giftId].push(contribution);
        return accumulator;
      }, {});
    });
    const isApprovedParticipant = computed(() => {
      return userParticipation.value?.status === "approved";
    });
    const approvedParticipants = computed(() => {
      return participants.value.filter((participant) => participant.status === "approved");
    });
    const secretSantaParticipants = computed(() => {
      if (!wishlist.value || wishlist.value.secretAssignmentMode === "disabled") {
        return approvedParticipants.value;
      }
      const ownerParticipant = {
        id: `owner-${wishlist.value.ownerId}`,
        userId: wishlist.value.ownerId,
        profile: ownerProfile.value ? {
          id: ownerProfile.value.id,
          full_name: ownerProfile.value.full_name,
          username: ownerProfile.value.username,
          avatar_url: ownerProfile.value.avatar_url,
          email: ownerProfile.value.email
        } : null
      };
      if (approvedParticipants.value.some((participant) => participant.userId === wishlist.value?.ownerId)) {
        return approvedParticipants.value;
      }
      return [ownerParticipant, ...approvedParticipants.value];
    });
    const distributionActive = computed(() => {
      return assignments.value.length > 0;
    });
    const participantManagementLocked = computed(() => {
      return wishlist.value?.secretAssignmentMode !== "disabled" && distributionActive.value;
    });
    const canAddGift = computed(() => {
      if (!wishlist.value) {
        return false;
      }
      return isOwner.value || isApprovedParticipant.value && (wishlist.value.wishlistCollaboration === "collaborative" || wishlist.value.secretAssignmentMode !== "disabled");
    });
    const canUseReservations = computed(() => {
      if (!wishlist.value) {
        return false;
      }
      return !isOwner.value && isApprovedParticipant.value && wishlist.value.giftReservation === "enabled";
    });
    const canViewParticipants = computed(() => {
      if (!wishlist.value) {
        return false;
      }
      return isOwner.value || wishlist.value.participantVisibility === "visible" && (wishlist.value.visibility === "public" || isApprovedParticipant.value);
    });
    const visibleParticipants = computed(() => {
      return participants.value.filter((participant) => participant.status === "approved");
    });
    const contactableVisibleParticipants = computed(() => {
      return visibleParticipants.value.filter((participant) => {
        return participant.profile?.email && participant.profile.email !== currentUserEmail.value;
      });
    });
    const ownerContactableParticipants = computed(() => {
      return participants.value.filter((participant) => {
        return participant.status === "approved" && participant.profile?.email && participant.profile.email !== currentUserEmail.value;
      });
    });
    const ownAssignment = computed(() => {
      return assignments.value.find((assignment) => assignment.giverUserId === currentUserId.value) || null;
    });
    const canGenerateRandomAssignments = computed(() => {
      return Boolean(
        isOwner.value && wishlist.value && wishlist.value.secretAssignmentMode === "random_assignment" && secretSantaParticipants.value.length >= 2 && !distributionActive.value
      );
    });
    const canGenerateManualAssignments = computed(() => {
      return Boolean(
        isOwner.value && wishlist.value && wishlist.value.secretAssignmentMode === "manual_assignment" && secretSantaParticipants.value.length >= 2 && !distributionActive.value
      );
    });
    const ownerMailto = computed(() => {
      if (!wishlist.value || !ownerProfile.value?.email || ownerProfile.value.email === currentUserEmail.value) {
        return "";
      }
      return buildParticipantsMailto(wishlist.value.title, [ownerProfile.value.email]);
    });
    const manageableGiftIds = computed(() => {
      if (isOwner.value) {
        return gifts.value.map((gift) => gift.id);
      }
      if (!canAddGift.value) {
        return [];
      }
      return gifts.value.filter((gift) => gift.createdBy === currentUserId.value).map((gift) => gift.id);
    });
    const accessActionLabel = computed(() => {
      if (!wishlist.value) {
        return "";
      }
      return wishlist.value.participantJoinMode === "open" ? "Join wishlist" : "Request access";
    });
    const secretSantaStatusMessage = computed(() => {
      if (!wishlist.value || wishlist.value.secretAssignmentMode === "disabled") {
        return "";
      }
      if (distributionActive.value) {
        return "Distribution is active. Participant changes are locked until the author deletes the distribution.";
      }
      if (secretSantaParticipants.value.length < 2) {
        return "At least two participants are required before generating a Secret Santa distribution.";
      }
      return "Add and approve all participants first, then generate the Secret Santa distribution.";
    });
    const wishlistThemeStyle = computed(() => {
      return getWishlistThemeStyle(wishlist.value?.themeKey);
    });
    const wishlistTitleParts = computed(() => {
      return splitWishlistTitle(wishlist.value?.title || "");
    });
    const wishlistScenarioLabel = computed(() => {
      if (!wishlist.value) {
        return "";
      }
      return formatWishlistScenario(
        detectWishlistScenario({
          visibility: wishlist.value.visibility,
          giftReservation: wishlist.value.giftReservation,
          hideReservedGiftsFromAuthor: wishlist.value.hideReservedGiftsFromAuthor,
          splitGifts: wishlist.value.splitGifts,
          randomGiftSelection: wishlist.value.randomGiftSelection,
          participantVisibility: wishlist.value.participantVisibility,
          wishlistCollaboration: wishlist.value.wishlistCollaboration,
          giftStatusVisibility: wishlist.value.giftStatusVisibility,
          secretAssignmentMode: wishlist.value.secretAssignmentMode,
          participantJoinMode: wishlist.value.participantJoinMode
        })
      );
    });
    const wishlistHighlights = computed(() => {
      if (!wishlist.value) {
        return [];
      }
      return [
        {
          label: "Gifts",
          value: gifts.value.length,
          note: "Ideas currently inside this list"
        },
        {
          label: "People",
          value: visibleParticipants.value.length + (isOwner.value ? 1 : 0),
          note: "Confirmed people around this wishlist"
        },
        {
          label: "Sharing",
          value: formatWishlistVisibility(wishlist.value.visibility),
          note: "How broadly the list is meant to travel"
        }
      ];
    });
    const syncWishlistFormValues = () => {
      if (!wishlist.value) {
        wishlistValues.value = {};
        return;
      }
      wishlistValues.value = {
        title: wishlist.value.title,
        description: wishlist.value.description,
        visibility: wishlist.value.visibility,
        giftReservation: wishlist.value.giftReservation,
        hideReservedGiftsFromAuthor: wishlist.value.hideReservedGiftsFromAuthor,
        splitGifts: wishlist.value.splitGifts,
        randomGiftSelection: wishlist.value.randomGiftSelection,
        participantVisibility: wishlist.value.participantVisibility,
        wishlistCollaboration: wishlist.value.wishlistCollaboration,
        giftStatusVisibility: wishlist.value.giftStatusVisibility,
        eventDate: wishlist.value.eventDate,
        eventTime: wishlist.value.eventTime,
        eventTimezone: wishlist.value.eventTimezone,
        secretAssignmentMode: wishlist.value.secretAssignmentMode,
        participantJoinMode: wishlist.value.participantJoinMode,
        themeKey: wishlist.value.themeKey
      };
    };
    const syncManualAssignmentSelections = () => {
      if (distributionActive.value) {
        manualAssignmentSelections.value = assignments.value.reduce((accumulator, assignment) => {
          accumulator[assignment.giverUserId] = assignment.recipientUserId;
          return accumulator;
        }, {});
        return;
      }
      manualAssignmentSelections.value = secretSantaParticipants.value.reduce((accumulator, participant, index) => {
        const otherParticipants = secretSantaParticipants.value.filter((item) => item.userId !== participant.userId);
        const fallbackRecipient = otherParticipants[index % otherParticipants.length];
        accumulator[participant.userId] = fallbackRecipient?.userId || "";
        return accumulator;
      }, {});
    };
    const formatMoney = (amount, currency) => {
      const formattedAmount = new Intl.NumberFormat("uk-UA", {
        maximumFractionDigits: Number.isInteger(amount) ? 0 : 2,
        minimumFractionDigits: Number.isInteger(amount) ? 0 : 2
      }).format(amount);
      return `${formattedAmount} ${currencySymbolMap[currency]}`;
    };
    const buildContributorMailto = (giftTitle, emails) => {
      const uniqueEmails = [...new Set(emails.filter(Boolean))];
      if (!uniqueEmails.length) {
        return "";
      }
      const subject = encodeURIComponent(`Split gift coordination: ${giftTitle}`);
      const body = encodeURIComponent(
        `Hello,

I am reaching out about the split gift "${giftTitle}". Let's coordinate the contribution details here.
`
      );
      return `mailto:${uniqueEmails.join(",")}?subject=${subject}&body=${body}`;
    };
    const buildParticipantsMailto = (wishlistTitle, emails) => {
      const uniqueEmails = [...new Set(emails.filter(Boolean))];
      if (!uniqueEmails.length) {
        return "";
      }
      const subject = encodeURIComponent(`Wishlist coordination: ${wishlistTitle}`);
      const body = encodeURIComponent(
        `Hello,

I am reaching out about the wishlist "${wishlistTitle}". Let's coordinate the details here.
`
      );
      return `mailto:${uniqueEmails.join(",")}?subject=${subject}&body=${body}`;
    };
    const giftStateById = computed(() => {
      if (!wishlist.value) {
        return {};
      }
      return Object.fromEntries(gifts.value.map((gift) => {
        const giftReservations = reservationsByGiftId.value[gift.id] || [];
        const splitEnabled = wishlist.value.splitGifts === "enabled";
        const giftContributions = splitEnabled ? contributionsByGiftId.value[gift.id] || [] : [];
        const contributorIds = new Set(giftContributions.map((contribution) => contribution.contributorId));
        const ownReservation = giftReservations.find((reservation) => reservation.reservedBy === currentUserId.value);
        const ownContribution = splitEnabled ? giftContributions.find((contribution) => contribution.contributorId === currentUserId.value) : void 0;
        const ownFullReservation = splitEnabled ? Boolean(ownReservation && !ownContribution) : Boolean(ownReservation);
        const fullReservations = splitEnabled ? giftReservations.filter((reservation) => !contributorIds.has(reservation.reservedBy)) : giftReservations;
        const otherFullReservation = fullReservations.find((reservation) => reservation.reservedBy !== currentUserId.value);
        const reservedCount = giftReservations.length;
        const reservedByOthers = reservedCount > 0 && !ownReservation;
        const totalContributionAmount = giftContributions.reduce((sum, contribution) => sum + contribution.amount, 0);
        const isReservationHiddenFromOwner = isOwner.value && wishlist.value?.hideReservedGiftsFromAuthor;
        const hasSplitActivity = splitEnabled && giftContributions.length > 0;
        let reservationMessage = "";
        let contributionSummary = null;
        if (!isReservationHiddenFromOwner) {
          if (ownContribution) {
            reservationMessage = `You pledged ${formatMoney(ownContribution.amount, ownContribution.currency)}.`;
          } else if (ownFullReservation && splitEnabled) {
            reservationMessage = "You reserved this gift fully.";
          } else if (ownReservation) {
            reservationMessage = "You reserved this gift.";
          } else if (otherFullReservation) {
            reservationMessage = "This gift is fully reserved.";
          } else if (reservedCount > 0) {
            if (wishlist.value.giftStatusVisibility === "hidden") {
              reservationMessage = "Reservation details are hidden for this wishlist.";
            } else if (wishlist.value.giftStatusVisibility === "partial") {
              reservationMessage = "This gift is already reserved.";
            } else {
              if (splitEnabled && totalContributionAmount > 0) {
                reservationMessage = `Total pledged so far: ${formatMoney(totalContributionAmount, gift.currency)}.`;
              } else {
                reservationMessage = reservedCount === 1 ? "1 participant already reserved this gift." : `${reservedCount} participants already reserved this gift.`;
              }
            }
          }
        }
        if (splitEnabled && !isReservationHiddenFromOwner && !ownFullReservation && !otherFullReservation) {
          const contributorCount = giftContributions.length;
          const targetAmount = gift.price ?? 0;
          const progressPercent = targetAmount > 0 ? Math.min(100, Math.round(totalContributionAmount / targetAmount * 100)) : 0;
          const targetLabel = gift.price != null ? formatMoney(gift.price, gift.currency) : "Flexible amount";
          const collectedLabel = formatMoney(totalContributionAmount, gift.currency);
          contributionSummary = {
            targetLabel,
            collectedLabel,
            progressPercent,
            contributorCount,
            contributions: [],
            statusNote: void 0
          };
          if (wishlist.value.giftStatusVisibility === "full") {
            contributionSummary.contributions = giftContributions.map((contribution) => ({
              id: contribution.id,
              name: contribution.contributorProfile?.fullName || contribution.contributorProfile?.username || "Participant",
              username: contribution.contributorProfile?.username || void 0,
              amountLabel: formatMoney(contribution.amount, contribution.currency)
            }));
          } else if (wishlist.value.giftStatusVisibility === "partial") {
            contributionSummary.statusNote = contributorCount ? `${contributorCount} ${contributorCount === 1 ? "contribution has" : "contributions have"} already been added.` : "No contributions yet.";
          } else {
            contributionSummary.statusNote = "Contribution details are hidden for this wishlist.";
          }
        }
        const canReserveContribution = splitEnabled ? canUseReservations.value && !ownReservation && !ownContribution && !otherFullReservation && gift.createdBy !== currentUserId.value : canUseReservations.value && !ownReservation && gift.createdBy !== currentUserId.value && !reservedByOthers;
        const canReserveFully = splitEnabled && canUseReservations.value && !ownReservation && !ownContribution && !otherFullReservation && !hasSplitActivity && gift.createdBy !== currentUserId.value;
        const reserveDisabled = !canUseReservations.value || gift.createdBy === currentUserId.value || !splitEnabled && reservedByOthers || Boolean(otherFullReservation);
        const reserveFullyDisabled = !canReserveFully;
        const reservationActionLabel = splitEnabled ? "Contribute to gift" : "Reserve gift";
        return [
          gift.id,
          {
            reservationMessage,
            reservationActionLabel,
            canReserve: canReserveContribution,
            reserveDisabled,
            reserveFullyActionLabel: "Reserve fully",
            canReserveFully,
            reserveFullyDisabled,
            canCancelReservation: canUseReservations.value && (splitEnabled ? Boolean(ownContribution) || ownFullReservation : Boolean(ownReservation)),
            contributionSummary
          }
        ];
      }));
    });
    const randomEligibleGifts = computed(() => {
      return gifts.value.filter((gift) => {
        const state = giftStateById.value[gift.id];
        return Boolean(state?.canReserve) && !state?.reserveDisabled;
      });
    });
    const canUseRandomGiftSelection = computed(() => {
      if (!wishlist.value) {
        return false;
      }
      return canUseReservations.value && wishlist.value.randomGiftSelection === "enabled" && randomEligibleGifts.value.length > 0;
    });
    const splitGiftSummaries = computed(() => {
      if (!wishlist.value || wishlist.value.splitGifts !== "enabled" || !canViewGifts.value || isOwner.value && wishlist.value.hideReservedGiftsFromAuthor) {
        return [];
      }
      return gifts.value.map((gift) => {
        const giftContributions = contributionsByGiftId.value[gift.id] || [];
        const contributorIds = new Set(giftContributions.map((contribution) => contribution.contributorId));
        const giftReservations = reservationsByGiftId.value[gift.id] || [];
        const fullReservations = giftReservations.filter((reservation) => !contributorIds.has(reservation.reservedBy));
        const collectedAmount = giftContributions.reduce((sum, contribution) => sum + contribution.amount, 0);
        const targetAmount = gift.price ?? 0;
        const progressPercent = targetAmount > 0 ? Math.min(100, Math.round(collectedAmount / targetAmount * 100)) : 0;
        const canRevealContributors = wishlist.value.giftStatusVisibility === "full";
        const contributorEntries = canRevealContributors ? giftContributions.map((contribution) => ({
          id: contribution.id,
          name: contribution.contributorProfile?.fullName || contribution.contributorProfile?.username || "Participant",
          username: contribution.contributorProfile?.username || "",
          email: contribution.contributorProfile?.email || "",
          amountLabel: formatMoney(contribution.amount, contribution.currency),
          href: contribution.contributorProfile?.email ? buildContributorMailto(gift.title, [contribution.contributorProfile.email]) : ""
        })) : [];
        const contributorEmails = canRevealContributors ? contributorEntries.map((entry) => entry.email).filter((email) => email && email !== currentUserEmail.value) : [];
        const contactHref = canRevealContributors ? buildContributorMailto(gift.title, contributorEmails) : "";
        const statusNote = wishlist.value.giftStatusVisibility === "partial" ? "Contributor identities are hidden for this wishlist." : wishlist.value.giftStatusVisibility === "hidden" ? "Contribution details are hidden for this wishlist." : "";
        return {
          id: gift.id,
          title: gift.title,
          targetLabel: gift.price != null ? formatMoney(gift.price, gift.currency) : "Flexible amount",
          collectedLabel: formatMoney(collectedAmount, gift.currency),
          progressPercent,
          contributorCount: contributorEntries.length,
          contributors: contributorEntries,
          contactHref,
          statusNote,
          visibleContributorCount: giftContributions.length,
          hasFullReservation: fullReservations.length > 0
        };
      }).filter((summary) => summary.visibleContributorCount > 0 && !summary.hasFullReservation);
    });
    const giftSectionDescription = computed(() => {
      if (isOwner.value) {
        return "Add gifts to your wishlist and mark their priority.";
      }
      if (!wishlist.value) {
        return "Browse the gifts in this wishlist.";
      }
      if (isApprovedParticipant.value && wishlist.value.wishlistCollaboration === "collaborative") {
        return "You are a participant in this wishlist and can add your own gifts.";
      }
      return "Browse the gifts in this wishlist.";
    });
    const visibleParticipantsMailto = computed(() => {
      if (!wishlist.value) {
        return "";
      }
      return buildParticipantsMailto(
        wishlist.value.title,
        contactableVisibleParticipants.value.map((participant) => participant.profile?.email || "")
      );
    });
    const ownerParticipantsMailto = computed(() => {
      if (!wishlist.value) {
        return "";
      }
      return buildParticipantsMailto(
        wishlist.value.title,
        ownerContactableParticipants.value.map((participant) => participant.profile?.email || "")
      );
    });
    const canViewGifts = computed(() => {
      if (!wishlist.value) {
        return false;
      }
      return isOwner.value || wishlist.value.visibility === "public" || isApprovedParticipant.value;
    });
    const giftEmptyMessage = computed(() => {
      if (isOwner.value) {
        return "No gifts yet. Use the button above to add the first one.";
      }
      if (!canViewGifts.value) {
        if (userParticipation.value?.status === "pending") {
          return "You do not have access to this wishlist yet. Gifts will appear after the owner approves your request.";
        }
        if (userParticipation.value?.status === "invited") {
          return "You do not have access to this wishlist yet. Accept the invitation to see the gifts.";
        }
        if (userParticipation.value?.status === "rejected") {
          return "You do not have access to this wishlist right now. The owner previously rejected your request.";
        }
        return "You do not have access to this wishlist yet. Join it or request access to see the gifts.";
      }
      return "No gifts have been added yet.";
    });
    const loadGiftData = async (wishlistId) => {
      const giftsResponse = await getWishlistGifts(wishlistId);
      if (giftsResponse.error) {
        return {
          error: giftsResponse.error
        };
      }
      gifts.value = giftsResponse.data;
      const [reservationsResponse, contributionsResponse] = await Promise.all([
        getGiftReservations(gifts.value.map((gift) => gift.id)),
        getGiftContributions(gifts.value.map((gift) => gift.id))
      ]);
      if (!reservationsResponse.error) {
        reservations.value = reservationsResponse.data;
      }
      if (!contributionsResponse.error) {
        contributions.value = contributionsResponse.data;
      }
      return {
        error: reservationsResponse.error || contributionsResponse.error || null
      };
    };
    const loadWishlistPage = async () => {
      statusMessage.value = "";
      giftStatusMessage.value = "";
      isLoaded.value = false;
      const user = await getUser();
      if (!user) {
        await router.push("/login");
        return;
      }
      currentUserId.value = user.id;
      currentUserEmail.value = user.email || "";
      const { data, error } = await getWishlistById(String(route.params.id));
      if (error || !data) {
        statusMessage.value = error ? `Failed to load wishlist: ${error.message}` : "Failed to load wishlist.";
        return;
      }
      wishlist.value = data;
      isOwner.value = data.ownerId === user.id;
      ownerProfile.value = null;
      syncWishlistFormValues();
      const participationResponse = await getUserParticipation(data.id, user.id);
      if (!participationResponse.error) {
        userParticipation.value = participationResponse.data;
      }
      const ownerProfileResponse = await getProfilesByIds([data.ownerId]);
      if (!ownerProfileResponse.error) {
        ownerProfile.value = ownerProfileResponse.data[0] || null;
      }
      if (isOwner.value || data.participantVisibility === "visible" && (data.visibility === "public" || participationResponse.data?.status === "approved")) {
        const participantsResponse = await getWishlistParticipants(data.id);
        if (!participantsResponse.error) {
          participants.value = participantsResponse.data;
        } else {
          statusMessage.value = `Wishlist loaded, but participants could not be loaded: ${participantsResponse.error.message}`;
        }
      }
      syncManualAssignmentSelections();
      const giftDataResponse = await loadGiftData(data.id);
      if (giftDataResponse.error && !statusMessage.value) {
        statusMessage.value = `Wishlist loaded, but some gift data could not be loaded: ${giftDataResponse.error.message}`;
      }
      if (data.secretAssignmentMode !== "disabled") {
        const assignmentsResponse = isOwner.value ? await getWishlistAssignments(data.id) : await getOwnWishlistAssignment(data.id, user.id);
        if (!assignmentsResponse.error) {
          assignments.value = assignmentsResponse.data ? Array.isArray(assignmentsResponse.data) ? assignmentsResponse.data : [assignmentsResponse.data] : [];
        } else if (!statusMessage.value) {
          statusMessage.value = `Wishlist loaded, but assignments could not be loaded: ${assignmentsResponse.error.message}`;
        }
      }
      syncManualAssignmentSelections();
      isLoaded.value = true;
    };
    const handleSubmit = async (payload) => {
      statusMessage.value = "";
      if (!payload.title.trim()) {
        statusMessage.value = "Wishlist title is required";
        return;
      }
      const wasSplitEnabled = wishlist.value?.splitGifts === "enabled";
      const willDisableSplit = wasSplitEnabled && payload.splitGifts === "disabled";
      if (willDisableSplit && wishlist.value) {
        const cleanupResponse = await clearSplitStateForWishlist(wishlist.value.id);
        if (cleanupResponse.error) {
          console.error("Split reservation cleanup failed:", cleanupResponse.error);
          statusMessage.value = `Split mode could not be turned off because shared contributions and related reservations were not fully removed: ${cleanupResponse.error.message}`;
          return;
        }
      }
      const { error, data } = await updateWishlist(String(route.params.id), payload);
      if (error || !data) {
        console.error("Wishlist update failed:", error);
        statusMessage.value = error ? `Failed to update wishlist: ${error.message}` : "Failed to update wishlist.";
        return;
      }
      wishlist.value = data;
      syncWishlistFormValues();
      if (willDisableSplit) {
        await loadGiftData(data.id);
        statusMessage.value = "Wishlist updated successfully. Split contributions and related reservations were cleared.";
      } else {
        statusMessage.value = "Wishlist updated successfully.";
      }
      settingsFormKey.value += 1;
      showSettingsModal.value = false;
    };
    const distributionConfirmTitle = computed(() => {
      return distributionConfirmMode.value === "generate" ? "Generate Secret Santa distribution" : "Delete Secret Santa distribution";
    });
    const distributionConfirmDescription = computed(() => {
      return distributionConfirmMode.value === "generate" ? "Please confirm that all participants have joined. After the distribution is generated, participant changes will be locked until you delete the distribution." : "Delete the current Secret Santa distribution and unlock participant management?";
    });
    const actionConfirmTitle = computed(() => {
      return actionConfirmMode.value === "deleteWishlist" ? "Delete this wishlist?" : `Delete “${actionConfirmGiftTitle.value}”?`;
    });
    const actionConfirmDescription = computed(() => {
      return actionConfirmMode.value === "deleteWishlist" ? "This will permanently remove the wishlist, its gifts, participant history, and related reservations." : "This will permanently remove the gift together with any related reservation or split-contribution data.";
    });
    const resetGiftForm = () => {
      editingGiftId.value = "";
      giftValues.value = {};
      showGiftModal.value = false;
    };
    const handleGiftSubmit = async (payload) => {
      giftStatusMessage.value = "";
      if (!wishlist.value) {
        return;
      }
      if (!payload.title.trim()) {
        giftStatusMessage.value = "Gift name is required.";
        return;
      }
      const response = editingGiftId.value ? await updateGift(editingGiftId.value, payload) : await createGift(wishlist.value.id, currentUserId.value, payload);
      if (response.error) {
        console.error("Gift save failed:", response.error);
        giftStatusMessage.value = editingGiftId.value ? `Failed to update gift: ${response.error.message}` : `Failed to create gift: ${response.error.message}`;
        return;
      }
      giftStatusMessage.value = editingGiftId.value ? "Gift updated successfully." : "Gift created successfully.";
      editingGiftId.value = "";
      giftValues.value = {};
      showGiftModal.value = false;
      await loadGiftData(wishlist.value.id);
    };
    const handleGiftEdit = (gift) => {
      editingGiftId.value = gift.id;
      giftValues.value = {
        title: gift.title,
        description: gift.description,
        imageUrl: gift.imageUrl,
        price: gift.price,
        currency: gift.currency,
        priority: gift.priority
      };
      giftStatusMessage.value = "";
      showGiftModal.value = true;
    };
    const handleGiftDelete = async (gift) => {
      if (!wishlist.value) {
        return;
      }
      pendingDeleteGiftId.value = gift.id;
      actionConfirmGiftTitle.value = gift.title;
      actionConfirmMode.value = "deleteGift";
      showActionConfirmModal.value = true;
    };
    const handleReserveGift = async (gift) => {
      if (!canUseReservations.value) {
        return;
      }
      if (!wishlist.value) {
        return;
      }
      if (gift.createdBy === currentUserId.value) {
        giftStatusMessage.value = "You cannot reserve your own gift.";
        return;
      }
      if (wishlist.value.splitGifts === "enabled") {
        selectedContributionGift.value = gift;
        contributionAmount.value = gift.price ? String(gift.price) : "";
        contributionNote.value = "";
        giftStatusMessage.value = "";
        showContributionModal.value = true;
        return;
      }
      const response = await reserveGift(gift.id, currentUserId.value);
      if (response.error) {
        giftStatusMessage.value = `Failed to reserve gift: ${response.error.message}`;
        return;
      }
      await loadGiftData(wishlist.value.id);
      giftStatusMessage.value = "Gift reserved successfully.";
    };
    const handleReserveGiftFully = async (gift) => {
      if (!canUseReservations.value || !wishlist.value) {
        return;
      }
      if (gift.createdBy === currentUserId.value) {
        giftStatusMessage.value = "You cannot reserve your own gift.";
        return;
      }
      const response = await reserveGift(gift.id, currentUserId.value);
      if (response.error) {
        giftStatusMessage.value = `Failed to reserve gift fully: ${response.error.message}`;
        return;
      }
      await loadGiftData(wishlist.value.id);
      giftStatusMessage.value = "Gift reserved fully.";
    };
    const handleCancelGiftReservation = async (gift) => {
      if (!wishlist.value) {
        return;
      }
      const ownContribution = (contributionsByGiftId.value[gift.id] || []).find((contribution) => {
        return contribution.contributorId === currentUserId.value;
      });
      if (wishlist.value.splitGifts === "enabled" && ownContribution) {
        const contributionResponse = await cancelContribution(gift.id, currentUserId.value);
        if (contributionResponse.error) {
          giftStatusMessage.value = `Failed to cancel contribution: ${contributionResponse.error.message}`;
          return;
        }
      }
      const response = await cancelReservation(gift.id, currentUserId.value);
      if (response.error) {
        giftStatusMessage.value = `Failed to cancel reservation: ${response.error.message}`;
        return;
      }
      await loadGiftData(wishlist.value.id);
      giftStatusMessage.value = "Reservation cancelled successfully.";
    };
    const participationMessage = computed(() => {
      if (!userParticipation.value) {
        return "";
      }
      if (userParticipation.value.status === "approved") {
        return "You already have access to this wishlist.";
      }
      if (userParticipation.value.status === "invited") {
        return "You were invited to this wishlist.";
      }
      if (userParticipation.value.status === "pending") {
        return "Your access request is waiting for approval.";
      }
      if (userParticipation.value.status === "rejected") {
        return "Your previous request was rejected.";
      }
      return "";
    });
    watch(() => route.params.id, () => {
      loadWishlistPage();
    });
    return (_ctx, _push, _parent, _attrs) => {
      const _component_GiftList = __nuxt_component_0;
      const _component_NuxtLink = __nuxt_component_0$2;
      const _component_WishlistForm = __nuxt_component_2;
      const _component_GiftForm = __nuxt_component_3;
      _push(`<section${ssrRenderAttrs(mergeProps({
        class: "page-section wishlist-details-page",
        style: unref(wishlistThemeStyle)
      }, _attrs))} data-v-67507e6c>`);
      if (unref(wishlist)) {
        _push(`<div class="surface-card wishlist-details-page__themed-card wishlist-details-page__hero-card" data-v-67507e6c><div class="wishlist-details-page__hero" data-v-67507e6c><div data-v-67507e6c><h1 data-v-67507e6c>Wishlist details</h1><p class="wishlist-details-page__hero-title" data-v-67507e6c>`);
        if (unref(wishlistTitleParts).cover) {
          _push(`<span class="wishlist-details-page__hero-emoji" data-v-67507e6c>${ssrInterpolate(unref(wishlistTitleParts).cover)}</span>`);
        } else {
          _push(`<!---->`);
        }
        _push(`<span data-v-67507e6c>${ssrInterpolate(unref(wishlistTitleParts).text || unref(wishlist).title)}</span></p>`);
        if (unref(ownerProfile)) {
          _push(`<p class="wishlist-details-page__owner-note" data-v-67507e6c> Owned by <strong data-v-67507e6c>${ssrInterpolate(unref(ownerProfile).full_name || unref(ownerProfile).username)}</strong>`);
          if (unref(ownerProfile).username) {
            _push(`<span data-v-67507e6c> · @${ssrInterpolate(unref(ownerProfile).username)}</span>`);
          } else {
            _push(`<!---->`);
          }
          _push(`</p>`);
        } else {
          _push(`<!---->`);
        }
        _push(`<p data-v-67507e6c>${ssrInterpolate(unref(wishlist).description || "No description yet.")}</p></div><div class="wishlist-details-page__header-actions" data-v-67507e6c>`);
        if (!unref(isOwner) && unref(ownerMailto)) {
          _push(`<a${ssrRenderAttr("href", unref(ownerMailto))} class="wishlist-details-page__secondary-button wishlist-details-page__mail-link" data-v-67507e6c> Email wishlist owner </a>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(isOwner)) {
          _push(`<button type="button" class="wishlist-details-page__primary-button" data-v-67507e6c> Edit wishlist </button>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div></div><div class="wishlist-details-page__meta-grid" data-v-67507e6c><p data-v-67507e6c><strong data-v-67507e6c>Scenario:</strong> ${ssrInterpolate(unref(wishlistScenarioLabel))}</p><p data-v-67507e6c><strong data-v-67507e6c>Visibility:</strong> ${ssrInterpolate(unref(formatWishlistVisibility)(unref(wishlist).visibility))}</p><p data-v-67507e6c><strong data-v-67507e6c>Join mode:</strong> ${ssrInterpolate(unref(formatParticipantJoinMode)(unref(wishlist).participantJoinMode))}</p><p data-v-67507e6c><strong data-v-67507e6c>Gift reservation:</strong> ${ssrInterpolate(unref(formatGiftReservationSetting)(unref(wishlist).giftReservation))}</p><p data-v-67507e6c><strong data-v-67507e6c>Gift status visibility:</strong> ${ssrInterpolate(unref(formatGiftStatusVisibility)(unref(wishlist).giftStatusVisibility))}</p>`);
        if (unref(wishlist).eventDate) {
          _push(`<p data-v-67507e6c><strong data-v-67507e6c>Event date:</strong> ${ssrInterpolate(unref(wishlist).eventDate)}</p>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(wishlist).eventTime) {
          _push(`<p data-v-67507e6c><strong data-v-67507e6c>Event time:</strong> ${ssrInterpolate(unref(wishlist).eventTime)}</p>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(wishlist).eventTimezone) {
          _push(`<p data-v-67507e6c><strong data-v-67507e6c>Timezone:</strong> ${ssrInterpolate(unref(wishlist).eventTimezone)}</p>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div><div class="wishlist-details-page__highlight-grid" data-v-67507e6c><!--[-->`);
        ssrRenderList(unref(wishlistHighlights), (item) => {
          _push(`<article class="wishlist-details-page__highlight-card" data-v-67507e6c><strong data-v-67507e6c>${ssrInterpolate(item.value)}</strong><span data-v-67507e6c>${ssrInterpolate(item.label)}</span><p data-v-67507e6c>${ssrInterpolate(item.note)}</p></article>`);
        });
        _push(`<!--]--></div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(wishlist)) {
        _push(`<div class="surface-card wishlist-details-page__section wishlist-details-page__themed-card" data-v-67507e6c><div class="wishlist-details-page__section-header" data-v-67507e6c><div data-v-67507e6c><h2 data-v-67507e6c>Gifts</h2><p data-v-67507e6c>${ssrInterpolate(unref(giftSectionDescription))}</p></div><div class="wishlist-details-page__header-actions" data-v-67507e6c>`);
        if (unref(canUseRandomGiftSelection)) {
          _push(`<button type="button" class="wishlist-details-page__secondary-button" data-v-67507e6c> Pick a gift for me </button>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(canAddGift)) {
          _push(`<button type="button" class="wishlist-details-page__primary-button" data-v-67507e6c> Add gift </button>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div></div>`);
        _push(ssrRenderComponent(_component_GiftList, {
          gifts: unref(gifts),
          "can-manage": unref(isOwner),
          "manageable-gift-ids": unref(manageableGiftIds),
          "gift-states": unref(giftStateById),
          "empty-message": unref(giftEmptyMessage),
          onEdit: handleGiftEdit,
          onDelete: handleGiftDelete,
          onReserve: handleReserveGift,
          onReserveFully: handleReserveGiftFully,
          onCancelReservation: handleCancelGiftReservation
        }, null, _parent));
        if (unref(giftStatusMessage)) {
          _push(`<p class="wishlist-details-page__gift-status" data-v-67507e6c>${ssrInterpolate(unref(giftStatusMessage))}</p>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(wishlist) && unref(wishlist).splitGifts === "enabled" && unref(canViewGifts) && unref(splitGiftSummaries).length) {
        _push(`<div class="surface-card wishlist-details-page__section wishlist-details-page__themed-card" data-v-67507e6c><div class="wishlist-details-page__section-header" data-v-67507e6c><div data-v-67507e6c><h2 data-v-67507e6c>Split gift contributions</h2><p data-v-67507e6c>Track progress for shared gifts and coordinate contributions by email.</p></div></div><div class="wishlist-details-page__split-summary-list" data-v-67507e6c><!--[-->`);
        ssrRenderList(unref(splitGiftSummaries), (summary) => {
          _push(`<article class="wishlist-details-page__split-summary-card" data-v-67507e6c><div class="wishlist-details-page__split-summary-header" data-v-67507e6c><div data-v-67507e6c><h3 data-v-67507e6c>${ssrInterpolate(summary.title)}</h3><p data-v-67507e6c><strong data-v-67507e6c>Collected:</strong> ${ssrInterpolate(summary.collectedLabel)} <span data-v-67507e6c> of ${ssrInterpolate(summary.targetLabel)}</span></p></div>`);
          if (summary.contactHref) {
            _push(`<a${ssrRenderAttr("href", summary.contactHref)} class="wishlist-details-page__primary-button wishlist-details-page__mail-link" data-v-67507e6c> Email contributors </a>`);
          } else {
            _push(`<!---->`);
          }
          _push(`</div><div class="wishlist-details-page__split-progress" data-v-67507e6c><div class="wishlist-details-page__split-progress-bar" style="${ssrRenderStyle({ width: `${summary.progressPercent}%` })}" data-v-67507e6c></div></div><p class="wishlist-details-page__split-meta" data-v-67507e6c>${ssrInterpolate(summary.visibleContributorCount)} ${ssrInterpolate(summary.visibleContributorCount === 1 ? "contributor" : "contributors")}</p>`);
          if (summary.statusNote) {
            _push(`<p class="wishlist-details-page__split-note" data-v-67507e6c>${ssrInterpolate(summary.statusNote)}</p>`);
          } else {
            _push(`<!---->`);
          }
          if (summary.contributors.length) {
            _push(`<div class="wishlist-details-page__split-contributors" data-v-67507e6c><!--[-->`);
            ssrRenderList(summary.contributors, (contributor) => {
              _push(`<article class="wishlist-details-page__split-contributor" data-v-67507e6c><div data-v-67507e6c><strong data-v-67507e6c>${ssrInterpolate(contributor.name)}</strong>`);
              if (contributor.username) {
                _push(`<p data-v-67507e6c>@${ssrInterpolate(contributor.username)}</p>`);
              } else {
                _push(`<!---->`);
              }
              _push(`<p data-v-67507e6c>${ssrInterpolate(contributor.amountLabel)}</p></div>`);
              if (contributor.href) {
                _push(`<a${ssrRenderAttr("href", contributor.href)} class="wishlist-details-page__secondary-button wishlist-details-page__mail-link" data-v-67507e6c> Email </a>`);
              } else {
                _push(`<!---->`);
              }
              _push(`</article>`);
            });
            _push(`<!--]--></div>`);
          } else {
            _push(`<!---->`);
          }
          _push(`</article>`);
        });
        _push(`<!--]--></div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(wishlist) && !unref(isOwner) && unref(canViewParticipants)) {
        _push(`<div class="surface-card wishlist-details-page__section wishlist-details-page__themed-card" data-v-67507e6c><div class="wishlist-details-page__section-header" data-v-67507e6c><div data-v-67507e6c><h2 data-v-67507e6c>Participants</h2><p data-v-67507e6c>See who is already taking part in this wishlist.</p></div>`);
        if (unref(visibleParticipantsMailto)) {
          _push(`<a${ssrRenderAttr("href", unref(visibleParticipantsMailto))} class="wishlist-details-page__secondary-button wishlist-details-page__mail-link" data-v-67507e6c> Email participants </a>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
        if (unref(visibleParticipants).length) {
          _push(`<div class="wishlist-details-page__participant-list" data-v-67507e6c><!--[-->`);
          ssrRenderList(unref(visibleParticipants), (participant) => {
            _push(`<article class="wishlist-details-page__participant-card" data-v-67507e6c><div data-v-67507e6c><strong data-v-67507e6c>${ssrInterpolate(participant.profile?.full_name || "User")}</strong><p data-v-67507e6c>@${ssrInterpolate(participant.profile?.username || "unknown")}</p></div>`);
            if (participant.profile?.email && participant.profile.email !== unref(currentUserEmail)) {
              _push(`<a${ssrRenderAttr("href", buildParticipantsMailto(unref(wishlist).title, [participant.profile.email]))} class="wishlist-details-page__secondary-button wishlist-details-page__mail-link" data-v-67507e6c> Email </a>`);
            } else {
              _push(`<!---->`);
            }
            _push(`</article>`);
          });
          _push(`<!--]--></div>`);
        } else {
          _push(`<p data-v-67507e6c> No approved participants are visible yet. </p>`);
        }
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(wishlist) && unref(wishlist).secretAssignmentMode !== "disabled") {
        _push(`<div class="surface-card wishlist-details-page__section" data-v-67507e6c><div class="wishlist-details-page__section-header" data-v-67507e6c><div data-v-67507e6c><h2 data-v-67507e6c>Secret assignment</h2><p data-v-67507e6c><strong data-v-67507e6c>Scenario:</strong> ${ssrInterpolate(unref(wishlistScenarioLabel))}</p><p data-v-67507e6c>${ssrInterpolate(unref(secretSantaStatusMessage))}</p></div>`);
        if (unref(isOwner)) {
          _push(`<div class="wishlist-details-page__participant-actions" data-v-67507e6c>`);
          if (unref(canGenerateRandomAssignments) || unref(canGenerateManualAssignments)) {
            _push(`<button type="button" class="wishlist-details-page__primary-button" data-v-67507e6c> Generate distribution </button>`);
          } else {
            _push(`<!---->`);
          }
          if (unref(distributionActive)) {
            _push(`<button type="button" class="wishlist-details-page__secondary-button" data-v-67507e6c> Delete distribution </button>`);
          } else {
            _push(`<!---->`);
          }
          if (unref(distributionActive)) {
            _push(`<button type="button" class="wishlist-details-page__secondary-button" data-v-67507e6c>${ssrInterpolate(unref(showOwnerAssignments) ? "Hide distribution" : "Show distribution")}</button>`);
          } else {
            _push(`<!---->`);
          }
          _push(`</div>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
        if (unref(isOwner) && unref(wishlist).secretAssignmentMode === "manual_assignment" && !unref(distributionActive) && unref(secretSantaParticipants).length) {
          _push(`<div class="wishlist-details-page__manual-assignment-grid" data-v-67507e6c><!--[-->`);
          ssrRenderList(unref(secretSantaParticipants), (participant) => {
            _push(`<article class="wishlist-details-page__manual-assignment-card" data-v-67507e6c><div data-v-67507e6c><strong data-v-67507e6c>${ssrInterpolate(participant.profile?.full_name || "Participant")}</strong><p data-v-67507e6c>@${ssrInterpolate(participant.profile?.username || "unknown")}</p></div><label class="wishlist-details-page__manual-assignment-field" data-v-67507e6c><span data-v-67507e6c>Recipient</span><select data-v-67507e6c><option value="" disabled data-v-67507e6c${ssrIncludeBooleanAttr(Array.isArray(unref(manualAssignmentSelections)[participant.userId]) ? ssrLooseContain(unref(manualAssignmentSelections)[participant.userId], "") : ssrLooseEqual(unref(manualAssignmentSelections)[participant.userId], "")) ? " selected" : ""}>Select recipient</option><!--[-->`);
            ssrRenderList(unref(secretSantaParticipants).filter((item) => item.userId !== participant.userId), (recipient) => {
              _push(`<option${ssrRenderAttr("value", recipient.userId)} data-v-67507e6c${ssrIncludeBooleanAttr(Array.isArray(unref(manualAssignmentSelections)[participant.userId]) ? ssrLooseContain(unref(manualAssignmentSelections)[participant.userId], recipient.userId) : ssrLooseEqual(unref(manualAssignmentSelections)[participant.userId], recipient.userId)) ? " selected" : ""}>${ssrInterpolate(recipient.profile?.full_name || "Participant")} (@${ssrInterpolate(recipient.profile?.username || "unknown")}) </option>`);
            });
            _push(`<!--]--></select></label></article>`);
          });
          _push(`<!--]--></div>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(isOwner)) {
          _push(`<!--[-->`);
          if (unref(ownAssignment)) {
            _push(`<div class="wishlist-details-page__participant-card" data-v-67507e6c><div data-v-67507e6c><strong data-v-67507e6c>Your recipient:</strong><p data-v-67507e6c>${ssrInterpolate(unref(ownAssignment).recipientProfile?.full_name || "Hidden recipient")}</p><p data-v-67507e6c>@${ssrInterpolate(unref(ownAssignment).recipientProfile?.username || "unknown")}</p></div>`);
            if (unref(ownAssignment).recipientProfile?.username) {
              _push(ssrRenderComponent(_component_NuxtLink, {
                to: `/friends/${unref(ownAssignment).recipientProfile.username}`,
                class: "wishlist-details-page__secondary-button"
              }, {
                default: withCtx((_, _push2, _parent2, _scopeId) => {
                  if (_push2) {
                    _push2(` View profile `);
                  } else {
                    return [
                      createTextVNode(" View profile ")
                    ];
                  }
                }),
                _: 1
              }, _parent));
            } else {
              _push(`<!---->`);
            }
            _push(`</div>`);
          } else {
            _push(`<!---->`);
          }
          if (unref(assignments).length && unref(showOwnerAssignments)) {
            _push(`<div class="wishlist-details-page__participant-list" data-v-67507e6c><!--[-->`);
            ssrRenderList(unref(assignments), (assignment) => {
              _push(`<article class="wishlist-details-page__participant-card" data-v-67507e6c><div data-v-67507e6c><strong data-v-67507e6c>${ssrInterpolate(assignment.giverProfile?.full_name || "Participant")}</strong><p data-v-67507e6c>@${ssrInterpolate(assignment.giverProfile?.username || "unknown")}</p></div><div data-v-67507e6c><strong data-v-67507e6c>${ssrInterpolate(assignment.recipientProfile?.full_name || "Recipient")}</strong><p data-v-67507e6c>@${ssrInterpolate(assignment.recipientProfile?.username || "unknown")}</p></div></article>`);
            });
            _push(`<!--]--></div>`);
          } else if (unref(assignments).length && !unref(showOwnerAssignments)) {
            _push(`<p data-v-67507e6c> Distribution is ready. Use “Show distribution” to review the pairs. </p>`);
          } else {
            _push(`<p data-v-67507e6c> No secret assignments have been created yet. </p>`);
          }
          _push(`<!--]-->`);
        } else if (unref(isApprovedParticipant)) {
          _push(`<!--[-->`);
          if (unref(ownAssignment)) {
            _push(`<div class="wishlist-details-page__participant-card" data-v-67507e6c><div data-v-67507e6c><strong data-v-67507e6c>Your recipient:</strong><p data-v-67507e6c>${ssrInterpolate(unref(ownAssignment).recipientProfile?.full_name || "Hidden recipient")}</p><p data-v-67507e6c>@${ssrInterpolate(unref(ownAssignment).recipientProfile?.username || "unknown")}</p></div>`);
            if (unref(ownAssignment).recipientProfile?.username) {
              _push(ssrRenderComponent(_component_NuxtLink, {
                to: `/friends/${unref(ownAssignment).recipientProfile.username}`,
                class: "wishlist-details-page__secondary-button"
              }, {
                default: withCtx((_, _push2, _parent2, _scopeId) => {
                  if (_push2) {
                    _push2(` View recipient profile `);
                  } else {
                    return [
                      createTextVNode(" View recipient profile ")
                    ];
                  }
                }),
                _: 1
              }, _parent));
            } else {
              _push(`<!---->`);
            }
            _push(`</div>`);
          } else {
            _push(`<p data-v-67507e6c> Your recipient has not been assigned yet. </p>`);
          }
          _push(`<!--]-->`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(wishlist) && unref(isOwner)) {
        _push(`<div class="surface-card wishlist-details-page__section" data-v-67507e6c><h2 data-v-67507e6c>Invite users</h2><p data-v-67507e6c>Invite a user by username. The invitation will be stored in the wishlist participants table.</p>`);
        if (unref(participantManagementLocked)) {
          _push(`<p class="wishlist-details-page__split-note" data-v-67507e6c> Participant management is locked while Secret Santa distribution is active. </p>`);
        } else {
          _push(`<!---->`);
        }
        _push(`<div class="wishlist-details-page__invite-row" data-v-67507e6c><input${ssrRenderAttr("value", unref(inviteUsername))} type="text" placeholder="Enter username"${ssrIncludeBooleanAttr(unref(participantManagementLocked)) ? " disabled" : ""} data-v-67507e6c><button type="button"${ssrIncludeBooleanAttr(unref(participantManagementLocked)) ? " disabled" : ""} data-v-67507e6c> Invite user </button></div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(wishlist) && unref(isOwner)) {
        _push(`<div class="surface-card wishlist-details-page__section" data-v-67507e6c><h2 data-v-67507e6c>Pending join requests</h2>`);
        if (unref(participantManagementLocked)) {
          _push(`<p class="wishlist-details-page__split-note" data-v-67507e6c> Pending requests cannot be changed while Secret Santa distribution is active. </p>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(pendingParticipants).length) {
          _push(`<div class="wishlist-details-page__participant-list" data-v-67507e6c><!--[-->`);
          ssrRenderList(unref(pendingParticipants), (participant) => {
            _push(`<article class="wishlist-details-page__participant-card" data-v-67507e6c><div data-v-67507e6c><strong data-v-67507e6c>${ssrInterpolate(participant.profile?.full_name || "User")}</strong><p data-v-67507e6c>@${ssrInterpolate(participant.profile?.username || "unknown")}</p></div><div class="wishlist-details-page__participant-actions" data-v-67507e6c><button type="button"${ssrIncludeBooleanAttr(unref(participantManagementLocked)) ? " disabled" : ""} data-v-67507e6c> Approve </button><button type="button"${ssrIncludeBooleanAttr(unref(participantManagementLocked)) ? " disabled" : ""} data-v-67507e6c> Reject </button></div></article>`);
          });
          _push(`<!--]--></div>`);
        } else {
          _push(`<p data-v-67507e6c> No pending join requests yet. </p>`);
        }
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(wishlist) && unref(isOwner)) {
        _push(`<div class="surface-card wishlist-details-page__section" data-v-67507e6c><div class="wishlist-details-page__section-header" data-v-67507e6c><div data-v-67507e6c><h2 data-v-67507e6c>Invited and approved participants</h2><p data-v-67507e6c>Manage who already has access and contact approved participants if needed.</p>`);
        if (unref(participantManagementLocked)) {
          _push(`<p class="wishlist-details-page__split-note" data-v-67507e6c> Participant changes are locked while Secret Santa distribution is active. </p>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
        if (unref(ownerParticipantsMailto)) {
          _push(`<a${ssrRenderAttr("href", unref(ownerParticipantsMailto))} class="wishlist-details-page__secondary-button wishlist-details-page__mail-link" data-v-67507e6c> Email approved participants </a>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
        if (unref(invitedParticipants).length) {
          _push(`<div class="wishlist-details-page__participant-list" data-v-67507e6c><!--[-->`);
          ssrRenderList(unref(invitedParticipants), (participant) => {
            _push(`<article class="wishlist-details-page__participant-card" data-v-67507e6c><div data-v-67507e6c><strong data-v-67507e6c>${ssrInterpolate(participant.profile?.full_name || "User")}</strong><p data-v-67507e6c>@${ssrInterpolate(participant.profile?.username || "unknown")}</p><p data-v-67507e6c>Status: ${ssrInterpolate(unref(formatParticipantStatus)(participant.status))}</p></div><div class="wishlist-details-page__participant-actions" data-v-67507e6c>`);
            if (participant.status === "approved" && participant.profile?.email && participant.profile.email !== unref(currentUserEmail)) {
              _push(`<a${ssrRenderAttr("href", buildParticipantsMailto(unref(wishlist).title, [participant.profile.email]))} class="wishlist-details-page__secondary-button wishlist-details-page__mail-link" data-v-67507e6c> Email </a>`);
            } else {
              _push(`<!---->`);
            }
            _push(`<button type="button"${ssrIncludeBooleanAttr(unref(participantManagementLocked)) ? " disabled" : ""} data-v-67507e6c> Remove </button></div></article>`);
          });
          _push(`<!--]--></div>`);
        } else {
          _push(`<p data-v-67507e6c> No invitations or approved participants yet. </p>`);
        }
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(wishlist) && !unref(isOwner)) {
        _push(`<div class="surface-card wishlist-details-page__section" data-v-67507e6c><h2 data-v-67507e6c>Access</h2>`);
        if (unref(participationMessage)) {
          _push(`<p data-v-67507e6c>${ssrInterpolate(unref(participationMessage))}</p>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(participantManagementLocked)) {
          _push(`<p class="wishlist-details-page__split-note" data-v-67507e6c> This wishlist is not accepting participant changes while Secret Santa distribution is active. </p>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(userParticipation)?.status === "invited") {
          _push(`<button type="button" data-v-67507e6c> Accept invitation </button>`);
        } else {
          _push(`<!---->`);
        }
        if (unref(userParticipation)?.status === "invited") {
          _push(`<button type="button" data-v-67507e6c> Reject invitation </button>`);
        } else {
          _push(`<!---->`);
        }
        if (!unref(userParticipation) || unref(userParticipation).status === "rejected") {
          _push(`<button type="button"${ssrIncludeBooleanAttr(unref(participantManagementLocked)) ? " disabled" : ""} data-v-67507e6c>${ssrInterpolate(unref(accessActionLabel))}</button>`);
        } else {
          _push(`<!---->`);
        }
        _push(`</div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(isOwner)) {
        _push(`<div class="surface-card wishlist-details-page__section" data-v-67507e6c><button type="button" class="wishlist-details-page__danger" data-v-67507e6c> Delete wishlist </button></div>`);
      } else {
        _push(`<!---->`);
      }
      if (!unref(isLoaded) && !unref(statusMessage)) {
        _push(`<div class="surface-card" data-v-67507e6c><p data-v-67507e6c>Loading wishlist settings...</p></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(showSettingsModal)) {
        _push(`<div class="wishlist-details-page__modal-backdrop" data-v-67507e6c><div class="surface-card wishlist-details-page__modal" data-v-67507e6c><div class="wishlist-details-page__modal-header" data-v-67507e6c><div data-v-67507e6c><h2 data-v-67507e6c>Edit wishlist</h2><p data-v-67507e6c>Update the settings for this wishlist.</p></div><button type="button" class="wishlist-details-page__modal-close" data-v-67507e6c> Close </button></div>`);
        if (unref(isLoaded) && unref(isOwner)) {
          _push(ssrRenderComponent(_component_WishlistForm, {
            key: unref(settingsFormKey),
            "initial-values": unref(wishlistValues),
            "submit-label": "Save wishlist changes",
            onSubmit: handleSubmit
          }, null, _parent));
        } else {
          _push(`<!---->`);
        }
        _push(`</div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(showGiftModal)) {
        _push(`<div class="wishlist-details-page__modal-backdrop" data-v-67507e6c><div class="surface-card wishlist-details-page__modal" data-v-67507e6c><div class="wishlist-details-page__modal-header" data-v-67507e6c><div data-v-67507e6c><h2 data-v-67507e6c>${ssrInterpolate(unref(giftFormHeading))}</h2><p data-v-67507e6c>Add a new gift or update an existing one.</p></div><button type="button" class="wishlist-details-page__modal-close" data-v-67507e6c> Close </button></div>`);
        _push(ssrRenderComponent(_component_GiftForm, {
          "initial-values": unref(giftValues),
          "submit-label": unref(editingGiftId) ? "Save gift changes" : "Add gift",
          "error-message": unref(giftStatusMessage),
          onSave: handleGiftSubmit,
          onCancel: resetGiftForm
        }, null, _parent));
        _push(`</div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(showContributionModal)) {
        _push(`<div class="wishlist-details-page__modal-backdrop" data-v-67507e6c><div class="surface-card wishlist-details-page__modal" data-v-67507e6c><div class="wishlist-details-page__modal-header" data-v-67507e6c><div data-v-67507e6c><h2 data-v-67507e6c>Contribute to gift</h2><p data-v-67507e6c>${ssrInterpolate(unref(selectedContributionGift)?.title || "Selected gift")}</p></div><button type="button" class="wishlist-details-page__modal-close" data-v-67507e6c> Close </button></div><label class="wishlist-details-page__modal-field" data-v-67507e6c><span data-v-67507e6c>Contribution amount</span><input${ssrRenderAttr("value", unref(contributionAmount))} type="number" min="0" step="0.01" placeholder="500" data-v-67507e6c></label><label class="wishlist-details-page__modal-field" data-v-67507e6c><span data-v-67507e6c>Note</span><textarea rows="3" placeholder="Optional note for this contribution" data-v-67507e6c>${ssrInterpolate(unref(contributionNote))}</textarea></label><div class="wishlist-details-page__modal-actions" data-v-67507e6c><button type="button" class="wishlist-details-page__primary-button" data-v-67507e6c> Save contribution </button><button type="button" class="wishlist-details-page__secondary-button" data-v-67507e6c> Cancel </button></div></div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(showDistributionConfirmModal)) {
        _push(`<div class="wishlist-details-page__modal-backdrop" data-v-67507e6c><div class="surface-card wishlist-details-page__confirm-modal" data-v-67507e6c><div class="wishlist-details-page__modal-header" data-v-67507e6c><div data-v-67507e6c><h3 data-v-67507e6c>${ssrInterpolate(unref(distributionConfirmTitle))}</h3><p data-v-67507e6c>${ssrInterpolate(unref(distributionConfirmDescription))}</p></div><button type="button" class="wishlist-details-page__modal-close" data-v-67507e6c> Close </button></div><div class="wishlist-details-page__confirm-actions" data-v-67507e6c><button type="button" class="wishlist-details-page__secondary-button" data-v-67507e6c> Cancel </button><button type="button" class="wishlist-details-page__primary-button" data-v-67507e6c>${ssrInterpolate(unref(distributionConfirmMode) === "generate" ? "Confirm distribution" : "Delete distribution")}</button></div></div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(showActionConfirmModal)) {
        _push(`<div class="wishlist-details-page__modal-backdrop" data-v-67507e6c><div class="surface-card wishlist-details-page__confirm-modal" data-v-67507e6c><div class="wishlist-details-page__modal-header" data-v-67507e6c><div data-v-67507e6c><h3 data-v-67507e6c>${ssrInterpolate(unref(actionConfirmTitle))}</h3><p data-v-67507e6c>${ssrInterpolate(unref(actionConfirmDescription))}</p></div><button type="button" class="wishlist-details-page__modal-close" data-v-67507e6c> Close </button></div><div class="wishlist-details-page__confirm-actions" data-v-67507e6c><button type="button" class="wishlist-details-page__secondary-button" data-v-67507e6c> Cancel </button><button type="button" class="wishlist-details-page__danger-button" data-v-67507e6c>${ssrInterpolate(unref(actionConfirmMode) === "deleteWishlist" ? "Delete wishlist" : "Delete gift")}</button></div></div></div>`);
      } else {
        _push(`<!---->`);
      }
      if (unref(statusMessage)) {
        _push(`<p data-v-67507e6c>${ssrInterpolate(unref(statusMessage))}</p>`);
      } else {
        _push(`<!---->`);
      }
      _push(`<div class="surface-card" data-v-67507e6c>`);
      _push(ssrRenderComponent(_component_NuxtLink, {
        to: "/wishlists",
        class: "wishlist-details-page__button-link"
      }, {
        default: withCtx((_, _push2, _parent2, _scopeId) => {
          if (_push2) {
            _push2(` Back to wishlists `);
          } else {
            return [
              createTextVNode(" Back to wishlists ")
            ];
          }
        }),
        _: 1
      }, _parent));
      _push(`</div></section>`);
    };
  }
});
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("pages/wishlists/[id].vue");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _id_ = /* @__PURE__ */ _export_sfc(_sfc_main, [["__scopeId", "data-v-67507e6c"]]);
export {
  _id_ as default
};
//# sourceMappingURL=_id_-B83mElN9.js.map
